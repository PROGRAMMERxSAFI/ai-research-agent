# AI-research-agent
Built an autonomous AI research agent capable of breaking down complex tasks into multi-step plans, executing them using web search and reasoning, and generating structured outputs.  * Implemented task planning using LLM prompts * Designed execution loop with tool usage (search + reasoning) * Added memory system to retain intermediate results 
